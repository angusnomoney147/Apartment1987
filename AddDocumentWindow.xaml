<Window x:Class="ApartmentManagementSystem.AddDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Document" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock Text="Add Document" 
                      Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Border>

        <!-- Form -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Document Name -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Document Name *" Margin="0,5"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtDocumentName" Margin="10,0,0,15" Padding="5"/>

            <!-- Document Type -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Document Type *" Margin="0,5"/>
            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CmbDocumentType" Margin="10,0,0,15" Padding="5"/>

            <!-- Notes -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Notes" Margin="0,5"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtNotes" Margin="10,0,0,15" Padding="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
        </Grid>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Cancel" 
                        Width="100" Height="35" Margin="0,0,10,0"
                        Background="#95A5A6" Foreground="White"
                        Click="BtnCancel_Click"/>
                <Button x:Name="BtnSave" Content="Save Document" 
                        Width="120" Height="35"
                        Background="#27AE60" Foreground="White"
                        Click="BtnSave_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>