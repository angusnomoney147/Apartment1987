<Window x:Class="ApartmentManagementSystem.AddEditPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Payment" Height="550" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock x:Name="TxtHeader" Text="Add New Payment" 
                      Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Lease Selection -->
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Lease *" Margin="0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="CmbLeases" Margin="0,0,0,15" Padding="5"/>

                <!-- Amount -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Amount ($)" Margin="0,5"/>
                <TextBox Grid.Row="3" Grid.Column="0" x:Name="TxtAmount" Margin="0,0,10,15" Padding="5" PreviewTextInput="DecimalOnly_PreviewTextInput"/>

                <!-- Payment Date -->
                <TextBlock Grid.Row="2" Grid.Column="1" Text="Payment Date *" Margin="0,5"/>
                <DatePicker Grid.Row="3" Grid.Column="1" x:Name="DpPaymentDate" Margin="0,0,0,15" Padding="5"/>

                <!-- Payment Method -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Payment Method" Margin="0,5"/>
                <ComboBox Grid.Row="5" Grid.Column="0" x:Name="CmbMethod" Margin="0,0,10,15" Padding="5"/>

                <!-- Reference Number -->
                <TextBlock Grid.Row="4" Grid.Column="1" Text="Reference Number" Margin="0,5"/>
                <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtReferenceNumber" Margin="0,0,0,15" Padding="5"/>

                <!-- Status -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Status" Margin="0,5"/>
                <ComboBox Grid.Row="7" Grid.Column="0" x:Name="CmbStatus" Margin="0,0,10,15" Padding="5"/>

                <!-- Notes -->
                <TextBlock Grid.Row="6" Grid.Column="1" Text="Notes" Margin="0,5"/>
                <TextBox Grid.Row="7" Grid.Column="1" x:Name="TxtNotes" Margin="0,0,0,15" Padding="5"/>
            </Grid>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Cancel" 
                        Width="100" Height="35" Margin="0,0,10,0"
                        Background="#95A5A6" Foreground="White"
                        Click="BtnCancel_Click"/>
                <Button x:Name="BtnSave" Content="Save Payment" 
                        Width="120" Height="35"
                        Background="#27AE60" Foreground="White"
                        Click="BtnSave_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>